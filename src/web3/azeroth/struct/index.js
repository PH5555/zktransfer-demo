import Key from './keys';
import Note from './note';

import ZkTransferParam from './zkTransferParam';
import ZkTransferEvent from './zkTransferEvent';
import mtree from './mtree';

const structure = {
    Key,
    Note,
    mtree,
    ZkTransferParam,
    ZkTransferEvent,
};

export default structure;